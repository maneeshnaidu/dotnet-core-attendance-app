@model List<Attendance>

@{
    ViewData["Title"] = "Index";
}

<h3 class="display-4">Attendance</h3>
    <div class="row">
        <a class="btn btn-primary" style="width:auto"
               asp-area="" asp-controller="Attendance" asp-action="Create">
                Add new attendance
            </a>
    </div>

@if (Model.Any())
{
    <table class="table table-striped table-bordered" style="width:100%" id="usersTable">
        <thead>
            <tr>
                <th>Time</th>
                <th>Type</th>
                <th>Employee</th>
            </tr>            
        </thead>
        <tbody>
            @foreach (var attendance in Model)
            {
<tr>
    <td>@attendance.AttendTime</td>
    <td>@attendance.AttendType</td>
    <td>@attendance.Employee.FirstName</td>
</tr>
            }
        </tbody>
    </table> 
    
}
else
{
    <div class="card">
        <div class="card-header">
            No users created yet.
        </div>
        <div class="card-body">
            <h5 class="card-title">
                Use the button below to create a user.
            </h5>
            <a class="btn btn-primary" style="width:auto"
               asp-area="" asp-controller="Employee" asp-action="Create">
                Add new employee
            </a>
        </div>
    </div>
}

@section Scripts{ 
    <script>


    $(document).ready(function () {
        $('#usersTable').DataTable({
            dom: 'Bfrtip',
            buttons: [
                'copy', 'csv',  'print'
            ]
        });
    });

    </script>
}
